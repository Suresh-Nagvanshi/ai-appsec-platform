# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# ── AI — Groq direct + LangChain integration ─────────────────────────────────
groq==0.9.0
langchain==0.3.25
langchain-core==0.3.65
langchain-community==0.3.24
langchain-groq==0.3.2

# ── RAG — Vector store + Embeddings ──────────────────────────────────────────
# chromadb 0.5.7 is the last version that bundles a pre-built hnswlib wheel
# for Python 3.13 on Windows — versions 0.5.8+ require compiling chroma-hnswlib
# from C++ source (needs MSVC / Visual Studio Build Tools).
chromadb==0.5.7
sentence-transformers==3.4.1
# langchain-huggingface replaces the deprecated langchain_community.embeddings
# HuggingFaceEmbeddings path (deprecated in LangChain 0.2.2, removed in 1.0).
langchain-huggingface==0.1.2
# numpy>=2.1.0 ships pre-built wheels for Python 3.13 on Windows —
# no C compiler / Visual Studio required.
numpy>=2.1.0,<3.0.0

# ── Git & file handling ───────────────────────────────────────────────────────
gitpython==3.1.43

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.35
alembic==1.13.2
# psycopg2-binary 2.9.10 ships a pre-built win_amd64 wheel for Python 3.13.
# 2.9.9 did not include a cp313 wheel and fell back to compiling from source.
psycopg2-binary==2.9.10

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic==2.9.2
httpx==0.27.2
