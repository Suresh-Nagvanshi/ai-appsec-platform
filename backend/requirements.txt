# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.1
python-multipart>=0.0.9

# AI / LLM
groq>=0.9.0

# Security scanning
GitPython>=3.1.43
semgrep>=1.70.0

# Validation
pydantic>=2.7.0

# Database — Phase 1 (ORM models + migrations)
# psycopg is the PostgreSQL driver (v3); [binary] ships the compiled C extension.
# If you prefer the pure-Python driver, use psycopg[c] or just psycopg.
SQLAlchemy>=2.0.0
alembic>=1.13.0
psycopg[binary]>=3.1.0
